const { User, Thought } = require('../models/index');

const thoughtController = {
    //get all thoughts 
    
    // get thoughts by ID

    // create new thought

    // update thought by ID

    //delete thought from user ID

    // add reaction by updating existing thought

    // remove reaction from thought 
}